# OPEN-ITEMS SWEEP (R-E1-CLEAR-THE-QUEUE 4)

_Generated 2026-09-18. Read LEDGER.md and LEAD_RULINGS.md; every ruling item is either DONE (shipped — the
ledger shows a commit per ruling) or listed below. Items needing no owner input are CLOSED here; the rest
are a short table of what genuinely waits on someone. RECORD-ONLY._

## Closed this turn (no owner input needed)

- **R-E1-REGIME-PERSISTENCE / R-E9-PAIRS / R-E1-VOL-SIZING / R-E1-SLOW-TREND** — were deferred to "after the
  first live ✅", then superseded by R-E1-RUN-NOW; **all four run this turn** (regime-split NO, pairs LOSES,
  vol-sizing LOSES, slow-trend LOSES). CLOSED.
- **Moment light (5th endpoint)** — long flagged "unanswered"; **armed** in the live book via
  R-E1-MOMENT-LIGHT (reads the granted read-only Kite quote/depth, fail-closed). CLOSED.
- **SEASON RULER, E11 NORMAL/INVERSE, E11 vol-target** — built and **armed as logged shadows** beside the
  live book; they resolve themselves on live data (no owner action to build them). CLOSED as build items.
- Every prior dispatch ruling (the ~40 in this session + the R1–R21 architecture rulings) — DONE, one commit
  each in LEDGER.md; the honest-test series (two-tests, barrier-first, shrink, two-scores, institutional,
  fee-multiple, required-move, TT3, E10, E4-idea, E3-events, AFL cross-check, E7 removal, E11, E3-NSE) all
  shipped with published verdicts.

## The remainder — what still waits, on whom

| Item | What blocks it | Who owns it |
|---|---|---|
| **Single-stock option-chain data** (the E4/E10 SIZE signal — the strongest the system found, 62–63% OOS, currently un-tradeable) | a paid single-stock option-chain dataset — OR time: the R-E4-RECORD-LIVE recorder now accumulates it live on each Kite login | **owner** (spend) / self-resolving as the recorder fills |
| **Index inclusion/exclusion dates** (E3 index-change strategy) | no structured NSE feed exists (only PDF press releases); a paid index-reconstitution dataset would close it | **owner** (spend, or accept the wall) |
| **Cross-stock / cross-timeframe deep hunt — full completion** | ~35 days of compute on this laptop (checkpointed; the sweep auto-resumes 18:00 IST today). A cloud box finishes it far faster | **owner** (whether to move it to the cloud) |
| **Adoption of any shadow** (E10 NORMAL/INVERSE, E11 vol-target, SEASON — SEASON is the only one that beat flat, on 72 post-hoc calls) | a **live margin beyond noise** from the live shadows — needs the live 20-day book to run and accrue weeks of data | **owner** (later, on live evidence) — nothing to do now |
| **E1-LEAD-HANDOFF.md as the book's section 0** | the file is not on the Desktop yet (the book inserts it automatically once it exists) | **owner** (optional) |
| **h40/h60 fresh finder re-scan** (required-move long-horizon hunt used re-evaluated conditions, not a fresh scan) | multi-day compute on this laptop (same wall as the cross-stock hunt) | **owner** (cloud) / compute-walled |
| **Live trading / funding / real money** | an owner signature — reserved, never delegated (§21) | **owner** (only) |

## Not blocked, just routine

- The **live 20-day book, the option-chain recorder, and the moment light** activate on the owner's next
  Kite login in market hours — a routine daily action, not a blocked item.
- **Audit-queue items #1–#37** are queued for the fresh-eyes/B-series audit cadence — for a future audit
  session, not blocked on the owner.

_RECORD-ONLY. Every open item above is owner-gated (a spend, a decision, a signature, or a routine login);
nothing is silently stuck on the builder._
